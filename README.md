# littlecat (my imports to!)
A import that can do a lot of stuff like:
diceroll(DD="yes")
su()
suL() (it's shell but loading)
and more!
and do mind that there cat-os and stuff because it just more imports
